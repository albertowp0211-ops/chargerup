# Dependencias
node_modules/

# Build de producción
client/dist/

# Datos privados: nunca subir al repositorio
server/data/orders.json
server/data/emails/
server/data/email-config.json
server/data/admin-password.txt
server/data/stripe-config.json
server/data/db-config.json
server/data/telegram-config.json

# Herramientas locales (config del harness, no compartir)
.claude/

# Sistema
.DS_Store
Thumbs.db
